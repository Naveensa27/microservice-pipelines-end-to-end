apiVersion: v1
kind: Service
metadata:
  name: capstone-app-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: capstone-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080